export interface UserProfile {
    // empID: string;
    userName: string ;
    // fullname : string;
    email : string;
    name : string;
    password?: string;
    showPassword?: boolean;
    confirmPassword?: string;
    newPassword?: string;
    contact: string;
    role: string;
    status: string;
}

